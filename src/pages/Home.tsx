
import { Link } from "react-router-dom";
import PageLayout from "@/components/layout/PageLayout";
import Index from "./Index"; // Importing the Index component we just created

const Home = () => {
  return <Index />; // Simply render the Index component
};

export default Home;
